Camel-smtp is a project which provide a camel component which consume message via smtp.

With camel component its possible to startup some embedded smtp server and so inject messages into a camel route.